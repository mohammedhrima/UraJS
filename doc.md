+ Documentation:
    + SPA
    + routes:
        + routes.json
        + nested routes
        + default route
    + config.json
    + HTTP
    + if / loop tags
    + typexcript/javascript support
    + scss / css support:
        + global.scss (used for global variables)
