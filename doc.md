+ Documentation:
    + routes:
        + routes.json
        + nested routes
        + default route
    + config.json
    + HTTP
    + if / loop tags
    + typexcript/javascript support
    + scss / css support
