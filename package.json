{
  "type": "module",
  "scripts": {
    "ts": "npm run clear && npm run _copy && tsc -w",
    "clear": "rm -rf src-js/*",
    "_copy": "rsync -av --exclude='*.ts' --exclude='*.tsx' src-ts/ src-js/",
    "copy": "while true; do find src-ts -type f -newermt '-1 seconds' ! -name '*.ts' ! -name '*.tsx' -exec rsync -av --exclude='*.ts' --exclude='*.tsx' src-ts/ src-js/ \\;; sleep 1; done",
    "start": "live-server --port=17000 --cors",
    "newc": "python3 new.py component",
    "newp": "python3 new.py page",
    "dev": "npm run ts & npm run _copy"
  },
  "dependencies": {
    "typescript": "^5.5.4"
  }
}
