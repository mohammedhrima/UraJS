{
  "name": "MiniJs",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "serve": "nodemon server.js",
    "build:swc": "npx swc ./src -d dist --strip-leading-paths",
    "build:copy": "copyfiles -u 1 'src/**/*.{html,css}' dist",
    "watch:swc": "npx swc src -w -d dist --strip-leading-paths",
    "watch:copy": "chokidar 'src/**/*.{html,css}' -c 'npm run build:copy'",
    "watch": "npm run watch:swc & npm run watch:copy",
    "build": "npm run build:swc && npm run build:copy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3",
    "mime": "^4.0.3",
    "styled-components": "^6.1.11",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@swc/cli": "^0.4.0",
    "@swc/core": "^1.7.4",
    "chokidar-cli": "^3.0.0",
    "copyfiles": "^2.4.1",
    "nodemon": "^3.1.0"
  }
}
